apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  selector: 
    app: user-service
  ports:
    - protocol: 'TCP'
      port: 3002
      targetPort: 3002
    # - protocol: 'TCP'
    #   port: 443
    #   targetPort: 443
  type: LoadBalancer