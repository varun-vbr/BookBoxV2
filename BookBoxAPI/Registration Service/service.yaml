apiVersion: v1
kind: Service
metadata:
  name: registration-service
spec:
  selector: 
    app: registration-service
  ports:
    - protocol: 'TCP'
      port: 3001
      targetPort: 3001
    # - protocol: 'TCP'
    #   port: 443
    #   targetPort: 443
  type: LoadBalancer