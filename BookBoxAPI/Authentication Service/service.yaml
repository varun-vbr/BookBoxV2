apiVersion: v1
kind: Service
metadata:
  name: authentication-service
spec:
  selector: 
    app: authentication-service
  ports:
    - protocol: 'TCP'
      port: 3000
      targetPort: 3000
    # - protocol: 'TCP'
    #   port: 443
    #   targetPort: 443
  type: LoadBalancer